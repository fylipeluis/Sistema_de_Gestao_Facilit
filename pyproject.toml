[project]
name = "facilit"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "mysql-connector-python",
    "python-dotenv",
    "uvicorn",
    "python-dateutil",
]

[tool.vercel]
entrypoint = "backend.main:app"
